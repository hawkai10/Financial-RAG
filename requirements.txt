###############################################
# Minimal, deduplicated requirements for RAG  #
# Local-only, Flask API, Chroma child store   #
###############################################

# Core API
flask>=2.3.0
flask-cors>=4.0.0
python-dotenv>=1.0.0

# Async + storage
aiohttp>=3.8.0
aiosqlite>=0.19.0

# Numerical + data
numpy>=1.24.0
pandas>=2.0.0

# Retrieval
rank-bm25>=0.2.2
chromadb>=0.5.4

# Local models (embeddings + cross-encoder reranker)
torch>=2.1.0
sentence-transformers>=2.2.2
transformers>=4.35.2
safetensors>=0.4.0
marker-pdf[full]


# Optional dev tooling (uncomment if needed)
# pytest>=7.4.0
# black>=23.0.0
# flake8>=6.0.0

# Notes:
# - Qdrant/pgvector backends removed; we enforce Chroma child store.
# - FastAPI/Streamlit/OpenAI/LangChain and PDF/Docx parsers removed; we use Marker CLI for extraction.
# - Keep torch/sentence-transformers/transformers for local-only model loading.
